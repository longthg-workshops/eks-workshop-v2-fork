#!/bin/bash

set -Eeuo pipefail

ENVIRONMENT=${ENVIRONMENT:-"${HOME}/eks-workshop-aws"}

reset-environment

kubectl delete -k ${ENVIRONMENT}/environment/eks-workshop/manifests/base-application > /dev/null
